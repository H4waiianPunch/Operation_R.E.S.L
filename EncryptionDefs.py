Detect key
Enter username
Generate random 16char string and encrypt with public key(server held)
That encrypted file should then be decrypted with the private key(on USB). If good, allow in. If bad, deny.
Resulting message from lines 3/4 should be the same
If the same, allow access.
If 